# StayLens — 项目上下文文件

> 每次对话开始前必须读取本文件。
> 每次任务完成后必须在 `devlog.md` 末尾追加一条记录（格式见文末）。

---

## 项目简介

StayLens 是一个酒店住宿评价工具。用户通过多阶段向导对一次住宿体验进行结构化评分，系统输出带等级判断的评价文案，支持多种风格和导出方式。

- 线上地址：https://heyyeqi.github.io/StayLens/
- 仓库：Heyyeqi/StayLens（main 分支，唯一分支）
- 部署方式：GitHub Pages，直接发布根目录 `index.html`

---

## 技术栈

纯静态单文件项目，无构建流程，无依赖管理。

| 层 | 技术 |
|---|---|
| 结构/样式/逻辑 | 单文件 HTML（4854 行） |
| 持久化 | localStorage（草稿、对比记录） |
| 导出 | Blob + URL.createObjectURL（.txt 下载） |
| 部署 | GitHub Pages |

---

## 设计语言

- 字体：Cormorant Garamond / Tenor Sans / DM Mono
- 色调：暖沙色系（#F6F2EB 为主背景）
- 风格：零圆角、报告感排版，接近 Mandarin Oriental 而非 SaaS
- 原则：克制、精准、有密度，不做视觉噪音

**这套设计语言是项目核心资产，任何 UI 修改必须在此框架内进行。**

---

## 内部模块结构（均在 index.html 内）

| 模块 | 位置（行号参考） | 功能 |
|---|---|---|
| 设计系统 / 样式 | 顶部 CSS | 字体、颜色、卡片、分数条、结果页视觉 |
| 评价 schema | ~2600 | 维度定义、评分项、风险项、溢价条件、三种模式过滤规则 |
| 状态管理 | ~2970 | state 对象、visitedDims、currentStage |
| 流程控制 | ~3012, ~4441 | 多阶段向导、goNext/goPrev、进度条渲染 |
| 评分计算引擎 | ~3111, ~3141 | 均分计算、风险修正、价值判断修正、等级输出 |
| 阶段页面渲染 | ~3244–4219 | renderCoverStage / renderModeStage / renderContextStage / renderDimensionStage / renderRiskStage / renderValueStage / renderResultStage |
| 文案生成器 | ~4001, ~4215 | 多模式输出（专业/极简/朋友圈/小红书/平台短评）、多口吻风格 |
| 结果导出 | ~4459, ~4468 | 复制文本、下载 .txt |
| 对比记录 | ~4491 | localStorage 存储，最多 10 条，支持覆盖/删除/清空 |
| 草稿自动保存 | ~4741 | 自动保存进度、7 天过期、加载时提示恢复 |
| 滑杆 UI 定位 | ~4587 | slider thumb 位置计算、tick 对齐、跨浏览器适配 |

---

## 评价模式

三种模式，对应不同评分项过滤规则：

- **通用模式**：默认
- **度假模式**：强调环境、休闲设施、私密性
- **商旅模式**：强调位置、工作环境、效率

---

## 评分输出等级

强推荐 / 推荐 / 中性 / 不推荐

修正机制：fatal 风险、structural 风险、expectation 风险、价值判断溢价

---

## 已知问题（截至 2025-05-13）

| 问题 | 位置 | 状态 |
|---|---|---|
| 草稿恢复失败时无用户反馈，仅 console.error | index.html:4806 | 未处理 |
| 数据持久化完全依赖 localStorage，无 JSON 导入/导出 | 全局 | 未实现 |
| 多处 catch 静默失败，可观测性差 | 全局 | 未处理 |

---

## 开发规范

- **不拆分文件**：当前为单文件架构，除非有明确重构计划，否则保持单文件
- **设计语言不得随意修改**：字体、色系、圆角（零）、间距体系是既定规范
- **schema 修改须同步更新三种模式的过滤规则**
- **localStorage key 修改须保证草稿向前兼容或主动清除旧草稿**
- 每次任务完成后在 `devlog.md` 末尾追加记录

---

## devlog 追加格式

```
## YYYY-MM-DD 任务简述

### 做了什么
- ...

### 改动位置（行号）
- ...

### 遗留问题
- ...
```
